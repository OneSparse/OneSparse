name: Release

on:
  push:
    tags:
      - 'v*.*.*'

jobs:
  test:
    name: Run Tests
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run test suite
        run: ./test.sh
        timeout-minutes: 10

  # docker:
  #   name: Build and Push Docker Image
  #   runs-on: ubuntu-latest
  #   needs: test

  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v4

  #     - name: Set up Docker Buildx
  #       uses: docker/setup-buildx-action@v3

  #     - name: Log in to Docker Hub
  #       uses: docker/login-action@v3
  #       with:
  #         username: ${{ secrets.DOCKERHUB_USERNAME }}
  #         password: ${{ secrets.DOCKERHUB_TOKEN }}

  #     - name: Extract version from tag
  #       id: version
  #       run: echo "VERSION=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT

  #     - name: Build and push Docker image
  #       uses: docker/build-push-action@v5
  #       with:
  #         context: .
  #         file: ./Dockerfile-slim
  #         push: true
  #         tags: |
  #           onesparse/onesparse-slim:${{ steps.version.outputs.VERSION }}
  #           ${{ !contains(github.ref, 'alpha') && !contains(github.ref, 'beta') && !contains(github.ref, 'rc') && 'onesparse/onesparse-slim:latest' || '' }}
  #         cache-from: type=registry,ref=onesparse/onesparse-slim:latest
  #         cache-to: type=inline

  release:
    name: Create GitHub Release
    runs-on: ubuntu-latest
    needs: docker
    permissions:
      contents: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Check if pre-release
        id: prerelease
        run: |
          if [[ "${GITHUB_REF#refs/tags/}" =~ (alpha|beta|rc) ]]; then
            echo "PRERELEASE=true" >> $GITHUB_OUTPUT
          else
            echo "PRERELEASE=false" >> $GITHUB_OUTPUT
          fi

      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          if [ "${{ steps.prerelease.outputs.PRERELEASE }}" = "true" ]; then
            gh release create "${GITHUB_REF#refs/tags/}" \
              --title "${GITHUB_REF#refs/tags/}" \
              --generate-notes \
              --prerelease
          else
            gh release create "${GITHUB_REF#refs/tags/}" \
              --title "${GITHUB_REF#refs/tags/}" \
              --generate-notes
          fi
